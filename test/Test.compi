/*
class Main {
    void print_int(int i)extern;

    void main(){ 
        int i,res;
        res=1;
        for i=1, i<6{
            res=res*i;
        }
        print_int(res);
   //     res=3;
        res=3%2;
        print_int(res);
        return;
    }
	
}*/

/*class Main{

    void print_int(int i)extern;


    // Calculo fibonacci iterativo
    int fib(int n){
        int first,second,tmp;
        first = 0;
        second = 1;
        while n>0 {
            tmp = first+second;
            first = second;
            second = tmp;
            n -= 1;
        }
        return first;
    }
    
    void main(){
        int i,f;
        print_int(99);
        f=6;
        i=fib(6);
        print_int(i);

        //print_int(fib(7));
        //no esta funcionando el push para metodos
        //print_int(fib(f));

        //f+=1;
        //i=fib(f);
        //print_int(i);

        return;

    }
}*/

class Main{

    void print_int(int i)extern;
    
    //calculo fib iterativo sin funcion
    void main(){
        int n,first,second,tmp;
        p=100;
        n=6;
        first = 0;
        second = 1;
        while n>0 {
            tmp = first+second;
            first = second;
            second = tmp;
            n -= 1;
        }
        print_int(first);
        return;
    }
}

/*
**PROBLEMAS PARA FUNCIONAR CON FUNCIONES POR EL OFFSET
**PROBLEMAS PARA UTILIZAR PARAMETROS EN FUNCIONES "NO ENCUENTRA, NO EXISTE" 
**en base al buscarLoc creado en VisitorCI no ve el valor inicial en java
**al estar debajo, en el main. El Push en tres direcciones no encuentra la variable
*/